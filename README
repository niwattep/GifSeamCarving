An implementation of seam carving for gif image based on https://github.com/sameeptandon/python-seam-carving implementation. The program is not optimized and has long execution time per image.

command:
python CAIS.py -i input.gif -r 400 500 -o output.gif -g
